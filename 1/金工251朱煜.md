#1.R语⾔最最基本的数据结构是什么？        
向量
#可以⽤哪个函数构建它？                   
c()
#总共有哪些类型？请针对每⼀种类型都进⾏构建。
as.logical(c(0))
as.integer(c("2"))
as.double(c("3"))
as.character(c("4"))
#⽤什么函数可以查看该数据结构的类型？
typeof()
#如何按照序号提取当中的元素？请给出相关代码。
l1 <- c(1,2,3,4)
l1[1]
l1[1:4]
l1[-1]
#R语⾔中哪种数据结构可以包含任何其他类型的数据结构？
列表
#可以⽤哪个函数构建它？请进⾏构建。
l2 <-list(c("2","3"),c('3',"4"),c("1",'4')
#如何进⾏当中元素的提取？有⼏种⽅式？请分别给出代码。
l2[1]
a<-l2[[1]]
a[1]
#如果需要进⾏常规的数据统计分析，需要⽤到哪种数据结构？
数据框
#该种数据结构与前两种数据结构有什么联系？请尝试构建⼀个。
library(tibble)
df2 <- tibble(x = 1:3, y = letters[1:3])
typeof(df2)